<?php
$path_parts  = explode('/', trim($uri, '/'));
$depth       = count(array_filter($path_parts));
$base_prefix = str_repeat('../', $depth);
$username    = $_SESSION['username'] ?? 'admin';
?>

<header class="topbar">
    <div class="topbar-left">
        <button class="menu-toggle" aria-label="Toggle Menu" type="button">
            <i class="ph ph-list"></i>
        </button>

        <div class="flex-grow-1">
            <form class="d-flex" role="search" style="max-width: 300px;">
                <input
                    type="text"
                    class="form-control"
                    id="search-input"
                    placeholder="Search..."
                >
            </form>
        </div>
    </div>

    <div class="topbar-right">
        <div class="user-dropdown">
            <a href="javascript:void(0)" class="user-profile text-decoration-none">
                <img
                    src="<?php echo $base_prefix; ?>assets/images/avater.png"
                    alt="User"
                    class="user-avatar"
                >
                <p class="small mb-0">
                    <span class="d-none d-sm-inline">Welcome, </span>
                    <strong><?php echo htmlspecialchars($username); ?></strong>
                </p>
            </a>
        </div>
    </div>
</header>